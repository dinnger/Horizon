{
  "name": "horizon",
  "version": "1.0.0",
  "description": "Proyecto principal",
  "main": "index.js",
  "workspaces": [
    "client",
    "client/canvas",
    "server"
  ],
  "scripts": {
    "server:dev": "ts-node-dev --respawn --transpile-only ./server/src/index.ts",
    "server:update-nodes": "ts-node ./server/src/seeders/update-node-permissions.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "glob": "^11.0.3"
  }
}
